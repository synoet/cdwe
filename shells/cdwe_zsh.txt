function cdwe() {
  local old_dir="$PWD"

  builtin cd "$@" || return

  local new_dir="$PWD"

  local result
  result="$({{{exec_path}}} run --old_dir="$old_dir" --new_dir="$new_dir")"
  eval "${result}"
}
